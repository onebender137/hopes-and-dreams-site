# --- SYNDICATE CORE (Private) ---
.env
.env.template
auth/
tokens.json

# --- SYSTEM & PYTHON JUNK ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
hd_env/
.vscode/
.idea/
.DS_Store
*.Zone.Identifier

# --- BIG DATA (Local Only) ---
# We ignore the vector database because it's huge and Dink can rebuild it from the PDFs
vector_db/

# If your PDF library is over 500MB, uncomment the line below to stop pushing it
# knowledge_base/

# If your "wild" photos/videos get too heavy for GitHub's 100MB limit, uncomment below
# media/

# --- LOGS & MEMORY ---
# Keep your personal chat history and logs out of the public cloud
*.log
syndicate_uplink.log
replied_comments.json
chat_memory.json
posted_topics.json
nohup.out
syndicate_memory.db

# --- TEMPORARY ASSETS ---
temp/
output/
*.mp4
*.mp3
code
vscode_cli.tar.gz
*.before_*
chat_memory.json.poisoned
0
101021
FETCH_HEAD
main
articles_backup_footer_change/

# --- DSDA INFRASTRUCTURE (Private CEO bus) ---
dsda_bus.py
dsda_print.py

# Backup files from surgical edits
*.bak-*
*.bak
knowledge_base/
_patches_applied/
patch_*.py

# --- LOCAL-ONLY TOOLING & SCRATCH (added 2026-07-05) ---
# One-off backend tool scripts that live in the repo dir but must never ship.
# (Repo already tracks other .py, so we name these explicitly rather than *.py.)
distill_briefs.py
fb_tools.py
fix_obsidian_md.py
hopes_tools.py
kb_verify.py
make_ansi.py
tech_redact.py
hopes_bot_finalize.sh
ansilove.js
tech_publish (1).py

# Scratch / probe / preview patterns
*_test.py
*_test.html
*_probe.py
*-preview.html

# Disk snapshots (e.g. bot.py.predisk-1780230215)
*.predisk-*

# Stray social-card exports
cipher-fb-feed.png
cipher-square.png
